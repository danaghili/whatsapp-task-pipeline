# 9. Architecture decisions

**No ADRs exist yet.** This project was adopted into friday management on
2026-07-20; per the adoption contract, the decision log
(`docs/DECISIONS.md`) begins at adoption and backdates nothing — a project
that grew up without records now has records that tell the truth about that.

Where a "why" is provable from a pre-adoption artifact, the reference set
cites it directly (`docs/ARCHITECTURE.md`, `README.md`, code comments,
commit `cf3e698`). Everywhere else the honest marker applies:
**Rationale not captured — the artifact shows the choice, not the
alternatives weighed.**

ADRs will accrue under `docs/architecture/decisions/` as future increments
make decisions worth recording, each citing its `DECISIONS.md: D-NNNN`
source.
